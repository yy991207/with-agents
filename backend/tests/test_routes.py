"""routes 单元测试 [DEPRECATED]

旧用例围绕 /decide /retry-think 等 think 路由
新流程下这些路由已移除  整文件需要按 /select_reply /retry_reply /cancel 重写
"""

import pytest

pytest.skip(
    "think 路由下线后此文件需要重写  暂时跳过",
    allow_module_level=True,
)
