mongo:
  uri: mongodb://localhost:27017
  db: multi_chat

runtime:
  history_max_rounds: 10
  reply_flush_interval_ms: 200
  http_timeout_seconds: 30

server:
  host: 0.0.0.0
  port: 8002
  cors_origins:
    - http://localhost:5173
